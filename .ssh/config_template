# SSH Configuration Template
# Copy this to ~/.ssh/config and customize as needed
# 
# Security best practices:
# 1. Use SSH keys instead of passwords
# 2. Disable password authentication where possible
# 3. Use modern ciphers and algorithms
# 4. Enable connection multiplexing for performance

# Default settings for all hosts
Host *
    # Security settings
    Protocol 2
    AddKeysToAgent yes
    UseKeychain yes  # macOS only - remove on Linux
    IdentitiesOnly yes
    
    # Modern crypto
    KexAlgorithms curve25519-sha256,curve25519-sha256@libssh.org,diffie-hellman-group16-sha512,diffie-hellman-group18-sha512
    HostKeyAlgorithms rsa-sha2-512,rsa-sha2-256,ssh-ed25519
    Ciphers chacha20-poly1305@openssh.com,aes256-gcm@openssh.com,aes128-gcm@openssh.com,aes256-ctr,aes192-ctr,aes128-ctr
    MACs hmac-sha2-256-etm@openssh.com,hmac-sha2-512-etm@openssh.com,hmac-sha2-256,hmac-sha2-512
    
    # Connection settings
    TCPKeepAlive yes
    ServerAliveInterval 60
    ServerAliveCountMax 3
    
    # Performance settings
    Compression yes
    
    # Connection multiplexing (uncomment if not using wsl-ssh-pageant)
    # ControlMaster auto
    # ControlPath ~/.ssh/control-master-%r@%h:%p
    # ControlPersist 10s

# Example: Personal server
# Host myserver
#     HostName myserver.example.com
#     User myusername
#     Port 22
#     IdentityFile ~/.ssh/id_ed25519
#     PreferredAuthentications publickey

# Example: Work server through bastion
# Host work-server
#     HostName internal-server.company.com
#     User work-username
#     Port 22
#     ProxyJump bastion.company.com
#     IdentityFile ~/.ssh/work_key

# Example: GitHub
# Host github.com
#     HostName github.com
#     User git
#     IdentityFile ~/.ssh/id_ed25519
#     PreferredAuthentications publickey

# Example: Local development VM
# Host dev-vm
#     HostName 192.168.1.100
#     User developer
#     Port 22
#     IdentityFile ~/.ssh/id_rsa
#     StrictHostKeyChecking no  # Only for local VMs
#     UserKnownHostsFile /dev/null

# Example: Emergency access with password fallback
# Host emergency-server
#     HostName emergency.example.com
#     User admin
#     PreferredAuthentications publickey,password
#     PasswordAuthentication yes